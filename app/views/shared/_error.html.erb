<% if element.errors.any? %>
  <div>
    <h3 class="error_explanation"><%= pluralize(element.errors.count, "error") %>
        prohibited this post from being saved:</h3>
    <ul>
    <% element.errors.full_messages.each do |message| %>
      <li class="error_message"><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>